spring:
  application:
    name: advising-system-api
  main:
    web-application-type: servlet

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.advisingsystem: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
